// import api from './api';

export const leaveService = {
    getAll: async () => {
        // const response = await api.get('/leaves');
        // return response.data;
        return [];
    },
};
