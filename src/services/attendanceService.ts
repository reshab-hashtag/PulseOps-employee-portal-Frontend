// import api from './api';

export const attendanceService = {
    getAll: async () => {
        // const response = await api.get('/attendance');
        // return response.data;
        return [];
    },
};
